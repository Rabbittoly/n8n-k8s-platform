# Kubernetes user-specific files
.kube/
kubeconfig

# User's values files - we want to include template values in the repo
# but exclude user-specific values
/values.yaml

# Runtime and temporary files
tmp/

# Real secrets (example secrets should still be included)
.env
*.key
secrets/*.env

# Generated backups
backups/

# n8n data and logs that are created after deployment
data/
*.log
logs/

# Editor and OS specific files
.DS_Store
.idea/
.vscode/
*.swp
*.swo
*~

# Dependencies that should be installed, not committed
node_modules/
venv/
__pycache__/
*.py[cod]
